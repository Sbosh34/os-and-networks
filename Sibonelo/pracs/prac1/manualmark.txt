Manually Marked Questions
# Question four
(a) In the segment under "Transmission Control Protocol" > Flags we see that the flags A(ACK) and S(Syn) are set to (b) They used the Sequence number of the SYN(3805462820) then added +1 to it

# Question eight
Window buffer of server was always increasing and didn't decrease within these 4 packets, hence the sender was never throttled in this case

# Question nine
No, there were no retransmitted segments in trace. I checked for any TCP segments flagged as [TCP Retransmission], which identifies packets where the same sequence number was sent more than once. There were none in the trace.

# Question fifteen
In the IP Header, the flag, More Fragment is set to 0, meaning we don't expect any more fragments after this one and the fragment offset is 0.

# Question sixteen
The Identification, TTL and Header CheckSum values are changing

# Question seventeen
jjk

# Question eighteen
The identification number of each of these udp segments keeps on being incremented by 1.

# Question nineteen
ICMP, Internet Control Message Protocol

# Question twenty
no, they are showing different behaviour, this is because these identification numbers are generated by different routers

# Question twenty one
no, they are not similar, they are only similar if these iCMP  messages were sent by the same router

